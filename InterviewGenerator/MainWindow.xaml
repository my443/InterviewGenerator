<Window x:Class="InterviewGenerator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:InterviewGenerator"
        mc:Ignorable="d"
        Title="Interview Generator" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>
        <DockPanel Grid.Row="0" Grid.ColumnSpan="5">
            <Menu DockPanel.Dock="Top">
                <MenuItem Header="_File">
                    <MenuItem Header="_New File" />
                    <MenuItem Header="_Open File" />
                    <Separator/>
                    <MenuItem Header="E_xit" />
                </MenuItem>
                <MenuItem Header="Data">
                    <MenuItem Header="Import _Categories" />
                    <MenuItem Header="Import _Questions" />
                </MenuItem>
            </Menu>
        </DockPanel>
        <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="5" Orientation="Horizontal" HorizontalAlignment="Left" Margin="10">
            <Button Margin="5" Padding="5">Add Question To Interview</Button>
            <Button Margin="5" Padding="5">Save Interview Template</Button>
            <Button Margin="5" Padding="5">See Saved Templates</Button>
            <Button Margin="5" Padding="5">Export Interview</Button>
        </StackPanel>
        <Grid Grid.Row="2" Grid.ColumnSpan="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <!-- ListView 1 -->
                <ColumnDefinition Width="Auto" />
                <!-- Separator 1 -->
                <ColumnDefinition Width="*" />
                <!-- ListView 2 -->
                <ColumnDefinition Width="Auto" />
                <!-- Separator 2 -->
                <ColumnDefinition Width="*" />
                <!-- ListView 3 -->
            </Grid.ColumnDefinitions>
            <ListView  Grid.Column="0">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Categories" Width="200" DisplayMemberBinding="{Binding CategoryName}"/>
                    </GridView>
                </ListView.View>
            </ListView>
            <GridSplitter Grid.Column="1"
                  Width="5"
                  HorizontalAlignment="Stretch"
                  VerticalAlignment="Stretch"
                  ResizeBehavior="PreviousAndNext"
                  Background="LightGray"
                  ShowsPreview="True" />
            <ListView  Grid.Column="2">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Questions" Width="200" DisplayMemberBinding="{Binding CategoryName}"/>
                    </GridView>
                </ListView.View>
            </ListView>
            <GridSplitter Grid.Column="3"
                  Width="5"
                  HorizontalAlignment="Stretch"
                  VerticalAlignment="Stretch"
                  ResizeBehavior="PreviousAndNext"
                  Background="LightGray"
                  ShowsPreview="True" />
            <ListView  Grid.Column="4">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Interivew" Width="200" DisplayMemberBinding="{Binding CategoryName}"/>
                    </GridView>
                </ListView.View>
            </ListView>
        </Grid>
        <StatusBar Grid.Row="3" Grid.ColumnSpan="5">Status</StatusBar>
    </Grid>
</Window>
